
// get a original canvas called origin
// get config called cfg
// let optimizer = new Optimizer(origin, cfg)
// init a result canvas called result
// optimizer.onStep = fn which do with step in the looper
//     fn = (step) => result.drawStep(step)
// optimizer.start()